<!-- Footer -->
<footer id="footer" class="bg6 p-t-45 p-b-43 p-l-45 p-r-45">
 <div class="container">
  <div class="footer__columns flex-w p-b-90">
    <div class="w-size6 p-t-35 p-l-15 p-r-15 respon3">
      {% include 'logo' %}
      <div class="p-t-8">
        <p class="s-text7 w-size27">
          {{ section.settings.footer_about_us }}
        </p>
        <div class="footer__social-icons flex-m p-t-20">
          {% if section.settings.footer_social_enable %}
          {% include 'social-button' %}
          {% endif %}
        </div>
        <p class="p-t-20 s-text7 w-size27">
          {{ section.settings.footer_content_text }}
        </p>
      </div>
    </div>
    <div class="w-size7 p-t-30 p-l-15 p-r-15 respon4">
      <h4 class="s-text12 p-b-15">{{ linklists[section.settings.footer_menu_1].title }}</h4>
      <ul>
        {% for link in linklists[section.settings.footer_menu_1].links %}
        <li class="p-b-5"><a href="{{link.url}}" class="s-text7">{{link.title}}</a></li>
        {% endfor %}
      </ul>
    </div>
    <div class="w-size7 p-t-30 p-l-15 p-r-15 respon4">
      <h4 class="s-text12 p-b-15">{{ linklists[section.settings.footer_menu_2].title }}</h4>
      <ul>
        {% for link in linklists[section.settings.footer_menu_2].links %}
        <li class="p-b-5"><a href="{{link.url}}" class="s-text7">{{link.title}}</a></li>
        {% endfor %}
      </ul>
    </div>
    <div class="w-size6 p-t-30 p-l-15 p-r-15 respon3">
      {% if section.settings.footer_newsletter_enable %}
      {% include 'newsletter' %}
      {% endif %}
      <div class="t-center p-l-15 p-r-15 p-t-30">
        {% unless shop.enabled_payment_types != empty %}
        {% assign enabled_payment_types = 'visa,mastercard,amex,paypal' | remove: ' ' | split: ',' %}
        {% for type in enabled_payment_types %}
        <a class="f-payment h-size2 icon fa fa-cc-{{ type }} fa-2x" aria-hidden="true"></a>
        {% endfor %}
        {% endunless %}
      </div>
    </div>
  </div>
  </div>
</footer>
